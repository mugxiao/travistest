language: bash
dist: xenial

before_install:
- sudo chmod u+x .travis/*.sh
- /bin/bash .travis/setup.sh

script:
#- /bin/bash .travis/build.sh Singularity
# build 
- travis_wait 20 sudo singularity build Singularity.simg Singularity
# test
- /bin/bash .travis/test.sh
# push

